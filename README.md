# Library-Management-System
A RESTful backend API for a university **Library Management System** that handles users, books, borrowing, returns, and fines. The project focuses on real backend engineering concepts such as authentication, role-based authorization, database design, and business logic enforcement, with no frontend dependency.
